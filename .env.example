# =============================================================================
# Grampanchayat Bavi — environment template
# Copy to `.env` and fill from Supabase → Settings → API. Never commit `.env`.
# =============================================================================

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase: run supabase/full-setup.sql once (create buckets officials, leaders, works, events first)
